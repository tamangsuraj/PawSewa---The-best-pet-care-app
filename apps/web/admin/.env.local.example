# Same backend as user_app / vet_app / website — MUST match (no mixing prod API with local mobile).
# Backend uses MongoDB Atlas Pawsewa-Cluster. Standardize on one Ngrok URL + /api/v1.
NEXT_PUBLIC_API_URL=https://foveal-gummatous-karyn.ngrok-free.dev/api/v1
# Local-only fallback:
# NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
