# Same backend as user_app / vet_app / website — MUST match (no mixing prod API with local mobile).
# Backend uses MongoDB Atlas Pawsewa-Cluster. Use your tunnel/deployed URL including /api/v1.
# Mobile: same origin only — flutter run --dart-define=BASE_URL=https://your-tunnel.ngrok-free.dev
NEXT_PUBLIC_API_URL=https://your-ngrok-id.ngrok-free.dev/api/v1
