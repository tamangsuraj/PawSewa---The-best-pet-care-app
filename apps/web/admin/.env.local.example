# Same backend as user_app / vet_app / website — MUST match (no mixing prod API with local mobile).
# Local API: http://localhost:3000/api/v1
# Tunnel (ngrok): paste the same https://… URL you use in Flutter ApiConfig / BASE_URL.
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1
