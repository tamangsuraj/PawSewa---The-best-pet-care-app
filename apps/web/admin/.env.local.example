# Same backend as user_app / vet_app / website — MUST match (no mixing prod API with local mobile).
# Backend uses MongoDB Atlas Pawsewa-Cluster. Use your tunnel/deployed URL (…/api/v1).
NEXT_PUBLIC_API_URL=https://your-ngrok-id.ngrok-free.app/api/v1
