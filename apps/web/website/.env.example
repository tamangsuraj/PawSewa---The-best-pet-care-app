# Website (Next.js) environment variables.
# NOTE: This app never connects to MongoDB directly. It calls the backend API, which connects to MongoDB Atlas.

NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
